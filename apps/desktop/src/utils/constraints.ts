const vaultConfig = {
  autoLock: {
    enabled: true,
    timeout: 300_000, // 5 minutes
  },
};

const appConfig = {
  appName: "Crownix Vault",
  appVersion: "1.0.0",
};

export { vaultConfig, appConfig };
